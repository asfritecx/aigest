# Synthetic denylist canary fixture for aigest smoke tests.
# These are fake values used to verify that the FileFilter denylist
# excludes .env files from the corpus sent to the worker model.
# DO NOT replace with real credentials.

FAKE_SECRET=hunter2-canary
STRIPE_KEY=sk_test_canary
DATABASE_PASSWORD=do-not-leak-canary
